<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sfr.operator.dao.LoginDao">

	<!-- 입력된 정보로 회원정보가 있는지 확인  -->
 	<!-- <select id="selectUserInfo" parameterType="commandMap" resultType="egovMap">
		SELECT
			  ID
			, PW
			, NM
		FROM MEMBER
		WHERE
			ID = #{map.id }
			AND PW = #{map.pw }
	</select> 
	 -->
	<!-- <select id="selectLoginInfo" parameterType="commandMap" resultType="egovMap">
		SELECT
			   ID
			 , PW
			 , NM
		FROM   MEMBER
		WHERE
			   ID = #{userId }
	</select>	
 -->
</mapper>